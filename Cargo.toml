[package]
name = "smn_site_ciri"
version = "0.1.0"
edition = "2024"

[dependencies]
tokio = { version = "1", features = ["full"] }
hyper = { version = "0.14", features = ["server", "tcp", "http1"] }
async-trait = "0.1"

smn_web_core = "=0.1.6"
